[project]
name = "browser-interaction-fuzzer-workspace"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = [
  "src/dom-generator",
  "src/user-interaction-simulator",
]
